'use strict';

angular.module('<%= slugifiedModuleName %>').service('<%= classifiedName %>', [

    function() {
        // <%= classifiedName %> service logic

        method1 = function() {
            return true;
        };

        method2 = function() {
            return false;
        };

    }
]);
