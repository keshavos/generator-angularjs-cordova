'use strict';

angular.module('<%= slugifiedModuleName %>').factory('<%= classifiedName %>', [

    function() {
        // <%= classifiedName %> service logic

        return {
            someMethod: function() {
                return true;
            }
        };
    }
]);
